# Core dependencies (required)
numpy>=1.20.0
scikit-image>=0.19.0
scipy>=1.7.0
Pillow>=9.0.0  # For GUI and icon creation

# Optional dependencies
# rawpy>=0.18.0  # For RAW image support (.nef, .cr2, .arw, etc.)
#                # Note: May not be available for Python 3.14+
#                # Install separately: pip install rawpy
